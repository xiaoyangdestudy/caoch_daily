# 生产环境配置模板
# 复制此文件为 .env 并修改相应配置

PORT=3000
NODE_ENV=production

# JWT配置（生产环境必须使用强密钥！）
# 生成命令: openssl rand -base64 32
JWT_SECRET=PLEASE_REPLACE_WITH_SECURE_RANDOM_KEY_AT_LEAST_32_CHARACTERS
JWT_EXPIRES_IN=30d

# 数据库配置
DB_PATH=./data/coach_daily.db

# CORS配置（生产环境限制来源，替换为你的实际域名）
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
