# Coach Daily - 服务器端快速开始指南

## 🚀 快速开始（Windows本地开发）

### 方式一：使用快速启动脚本（推荐）

1. **双击运行** `start.bat`
   - 自动安装依赖
   - 自动启动开发服务器
   - 服务器将运行在 http://localhost:3000

### 方式二：手动启动

```powershell
# 1. 进入服务器目录
cd D:\00_DevProject\caoch_daily\server

# 2. 安装依赖（首次运行）
npm install

# 3. 启动开发服务器
npm run dev
```

## 🧪 测试API

### 自动测试

双击运行 `test-api.bat` 自动测试所有API端点

### 手动测试

#### 1. 健康检查
```powershell
curl http://localhost:3000/health
```

#### 2. 注册用户
```powershell
curl -X POST http://localhost:3000/api/auth/register `
  -H "Content-Type: application/json" `
  -d '{"username":"testuser","password":"test123"}'
```

#### 3. 登录
```powershell
curl -X POST http://localhost:3000/api/auth/login `
  -H "Content-Type: application/json" `
  -d '{"username":"testuser","password":"test123"}'
```

#### 4. 创建回顾记录（需要token）
```powershell
# 先登录获取token，然后：
curl -X POST http://localhost:3000/api/reviews `
  -H "Content-Type: application/json" `
  -H "Authorization: Bearer YOUR_TOKEN_HERE" `
  -d '{
    "id":"test-review-1",
    "date":"2024-01-20",
    "mood":"happy",
    "highlights":["完成了重要任务"],
    "improvements":["可以更早开始"],
    "tomorrowPlans":["继续努力"]
  }'
```

## 📱 推荐工具

### VS Code插件（强烈推荐）

1. **Thunder Client** - API测试工具
   - 安装：在VS Code扩展市场搜索"Thunder Client"
   - 优点：界面友好，直接在VS Code中使用
   - 使用：点击左侧雷电图标，创建请求测试API

2. **REST Client** - 文本形式的API测试
   - 在.http文件中编写请求
   - 点击"Send Request"即可测试

### 独立工具

- **Postman** - 功能最全的API测试工具
- **Insomnia** - 轻量级API测试工具

## 📂 项目结构

```
server/
├── src/
│   ├── config/
│   │   └── database.js       ← 数据库配置
│   ├── middleware/
│   │   ├── auth.js           ← JWT认证
│   │   └── errorHandler.js  ← 错误处理
│   ├── routes/
│   │   ├── auth.js           ← 认证API
│   │   ├── reviews.js        ← 每日回顾API
│   │   ├── workouts.js       ← 运动记录API
│   │   ├── meals.js          ← 饮食记录API
│   │   ├── sleep.js          ← 睡眠记录API
│   │   ├── focus.js          ← 专注工作API
│   │   └── sync.js           ← 数据同步API
│   ├── utils/
│   │   └── jwt.js            ← JWT工具
│   └── index.js              ← 主入口
├── data/
│   └── coach_daily_dev.db    ← SQLite数据库（自动创建）
├── logs/                      ← PM2日志（生产环境）
├── package.json
├── .env                       ← 开发环境配置
├── .env.production           ← 生产环境模板
├── .gitignore
├── ecosystem.config.js       ← PM2配置
├── deploy.sh                 ← Linux部署脚本
├── start.bat                 ← Windows快速启动
├── test-api.bat              ← Windows测试脚本
└── README.md
```

## 🔧 常见问题

### 1. 端口被占用

如果3000端口被占用，修改 `.env` 文件：
```
PORT=3001
```

### 2. npm install失败

```powershell
# 清除缓存重试
npm cache clean --force
npm install
```

### 3. 数据库文件位置

开发环境：`server/data/coach_daily_dev.db`

可以使用 **DB Browser for SQLite** 查看数据库内容

### 4. 查看日志

开发模式会在控制台直接显示所有日志

## 📖 API文档

查看 `README.md` 了解所有API端点和使用方法

## 🚢 部署到生产环境

查看根目录的文档：
- 《服务器部署配置文档.md》 - 完整部署指南
- 《开发部署工作流.md》 - 开发流程指南

## 💡 下一步

1. ✅ **本地测试成功** - 恭喜，服务器端已经完成！
2. 📱 **集成Flutter客户端** - 修改Flutter代码调用API
3. 🚀 **部署到云服务器** - 让数据同步到云端

## 🆘 需要帮助？

- 查看 `README.md` 了解API使用
- 查看根目录文档了解部署流程
- 遇到问题可以查看日志排查
